# Genesis II staging report

This report is a template for the required staging deployment evidence. It must
be completed from an actual Polkadot Hub TestNet deployment; no deployment
result may be inferred from local tests or UI configuration.

```text
contract:
deployment tx:
deployment block:
runtime code hash:
allocation:
start price:
end price:
start timestamp:
end timestamp:
EVM purchase txs:
Substrate purchase txs:
deadline rejection tx:
observed totals:
```

Current status: BLOCKED until staging credentials and a funded staging account
are supplied. The independent deployment tooling and local validation are
implemented in `scripts/deploy-phase2.mjs`.
